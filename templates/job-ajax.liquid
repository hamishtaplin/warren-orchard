{% layout "blank" %}
{% block content %} 

  <ul class="gallery">
    
    {% for job in global.jobs  %}
      {% if job.id == {{request.query.id}} %}
        {% for img in job.images %}
          <li class="gallery__item">
            {% capture myimg %}/public/images/content/fullsize/job-{{job.id}}/{{img}}.jpg{% endcapture %}
            {% if mixture.debug  %}
              <img data-src="{{ myimg }}" class="gallery__img  img" />
             {% else %}
              <img data-src="{{ myimg | asset_url }}" class="gallery__img  img" />
            {% endif %}   
          </li>
        {% endfor %}
      {% endif %}
    {% endfor %}
  </ul>    


{% endblock %}